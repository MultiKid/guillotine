# Server

Multiplayer and server logic are intentionally deferred until a later phase.

Do not add Socket.io handlers, room-code logic, backend runtimes, persistence, accounts, authentication, matchmaking, or deployment code in the Phase 1 scaffold.
